{
  "name": "nightcall",
  "version": "0.1.0",
  "private": true,
  "description": "A dark software house: a virtual AI company on llm4ts with Azure DevOps as its control plane and the Gemini CLI as its coder",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc -b tsconfig.json --pretty false",
    "test": "vitest run",
    "start": "node --env-file-if-exists=.env --experimental-strip-types src/main.ts"
  },
  "dependencies": {
    "@llm4ts/core": "0.13.1",
    "@llm4ts/flow": "0.13.1",
    "@llm4ts/runner": "0.13.1",
    "effect": "^4.0.0-beta.102"
  },
  "devDependencies": {
    "@effect/vitest": "beta",
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
